import { Typography } from '@material-ui/core';
import Layout from 'layouts/Authenticated';
import React from 'react';

const DeviceMap = () => {
  return (
    <Layout greeting="Mapas">
      <Typography variant="h4">Maps</Typography>
    </Layout>);
};

export default DeviceMap;